const fs = require('fs');
const path = require('path');
const puppeteer = require('puppeteer');
const handlebars = require('handlebars');

function generatePDFFromTemplate(htmlContent) {
  // ... Same PDF generation logic as before ...
}

module.exports = {
  generatePDFFromTemplate,
};
